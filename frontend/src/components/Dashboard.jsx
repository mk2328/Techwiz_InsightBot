import React from "react";
import ArticlesList from "./ArticlesList";

const Dashboard = () => {
  return (
    <section>
      <h1>Dashboard</h1>
      <ArticlesList />
    </section>
  );
};

export default Dashboard;
